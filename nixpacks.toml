# 1. Coolify'a bu projenin Python olduğunu zorluyoruz (Node.js karışıklığını çözer)
providers = ["python"]

# 2. Python sürümünü belirliyoruz
[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

# 3. Başlatma komutunu veriyoruz
[start]
cmd = "gunicorn -w 2 -b 0.0.0.0:3000 main:app"
